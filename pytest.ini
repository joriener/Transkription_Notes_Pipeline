# =============================================================
#  Transkription_Notes_Pipeline - pytest.ini
#  Pins the rootdir and where tests live, so the suite runs the same
#  way from any working directory.
#
#  Run: C:\Python\Python311\python.exe -m pytest
#       C:\Python\Python311\python.exe -m pytest tests/test_db.py -v
# =============================================================

[pytest]
testpaths = tests
